---
title: Customization
description: Customize ClawHalla and OpenClaw behavior.
---

## Customize scripts

Edit files in `scripts/` to adapt startup and reset flows.

## Customize Docker image

Tune `Dockerfile` and `docker-compose.yml` for your environment.

## Customize OpenClaw behavior

Use:

```bash
openclaw configure
openclaw agents add <name>
```
